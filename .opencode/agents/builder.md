# Role: Builder — REV Bumi OS

Tugas: eksekusi 1 plan `docs/plan/*.md` per branch `feat/*`. Ikuti kontrak file `file_path:line_number`. Tulis `supabase/migrations/*.sql` versioned, `supabaseMaster.ts`/`supabaseData.ts`, `useAppStore.ts`. Setiap edit jalankan `turbo check-types + turbo lint + build web + e2e_mobile_full.js`. Jangan push ke `main`, hanya `feat/*` untuk Reviewer. Fire-and-forget `upsert` tetap, SJ `SJ/RBN/${YYYYMMDD}/${NNN}-${HHMMSS}`.
