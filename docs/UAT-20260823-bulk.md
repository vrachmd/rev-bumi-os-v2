# UAT Bulk Ritase Massal — 2026-08-23

> Checklist manual di `app.revbuminusantara.biz.id` (web) + Expo HP (mobile). Akun: quarry `quarry@revbumi.co.id / Revbumi123!`, site `site@revbumi.co.id / Revbumi123!`, admin `ghifarisausans@gmail.com / Ucihavieri11`.

## Web — Ritase Massal (Bulk) `BulkDeliveriesView`
1. Login admin → Sidebar **Ritase Massal (Bulk)**.
2. Klik **Download Template CSV** → isi 10 baris: 5 `ALL_IN` (vendor IVAN, quarry Bojonegara, project Karya Beton) + 5 `PER_TRIP` (vendor Yudhi, quarry Rumpin, project Cisumdawu) — sertakan 1 baris sengaja error (quarry typo).
3. Drag-drop CSV → preview 20: cek badge `9 valid / 1 error`, vol `15.00 m³` & ongkos `ALL_IN 0 / PER_TRIP Rp 3.150.000` + chip hijau/biru.
4. Klik **Export error.csv** → cek 1 baris error `quarry tidak ketemu`.
5. Klik **Import 9 Ritase Valid** → progress `10→100%` → `Batch bulk-YYYYMMDD-XXXXX: 9 sukses`.
6. Kembali ke **Pengiriman & Surat Jalan** → filter `bulk_batch_id` (atau cari `SJ/BULK`) → 9 baru `SCHEDULED`.
7. Pilih 1 bulk → **Timbang** (gross 38t tare 14t) → `IN_TRANSIT` → cek `bulk_batch_id` tetap.

## Mobile — Bulk 10 Ritase `BulkQuarryScreen`
1. Login `quarry@revbumi.co.id` di Expo → tab **1. Quarry** → tombol **+ Bulk 10 Ritase** (atas).
2. Isi 3 baris valid (pilih quarry/product/vendor/plat/kontrak + supir) → 7 kosong dilewati → **Simpan 3 Ritase** → Alert `3 ritase batch bulk-...`.
3. Matikan data/online → isi 2 baris lagi → Simpan → cek banner `pendingCount 2` (offline queue).
4. Nyalakan online → banner hilang `pendingCount 0` → cek web cockpit 2 ritase muncul `SCHEDULED`.
5. Test plat baru: isi plat `B BULK 9999` yang belum ada → Simpan → cek di web `Master Kendaraan` plat baru ada (RLS 0012).

## Lanjut Alur 3-Role (1 sample bulk)
6. Quarry: `LOADING` → `IN_TRANSIT` (tanda tangan).
7. Site: login `site@...` → **2. Site** → `ARRIVED` (GPS dalam radius) → `UNLOADED` (ukur 14.5 m³) → `POD_SUBMITTED` (ttd).
8. Admin: verify `POD_VERIFIED` → `DELIVERED`.
9. Finance: **Faktur** → candidate hanya `DELIVERED approved>0` (bulk SCHEDULED tidak muncul) → buat faktur 1 ritase tadi → cek PDF `IMCI-AGREGAT` (agregat) vs `STANDARD-PER-RIT` & kwitansi hal 2 jika ada foto.

## Expected
- Web CSV error handling, mobile 10/20 limit, offline replay, 6 pricing model tetap (ALL_IN/PER_TRIP primary), bulk_batch_id index, audit BULK_CREATE.
- Tandai checklist lulus → tag `checkpoint-20260824-go-uat`.

## Akun & Env
- Vercel `rev-bumi-os-v2-web` auto-deploy `main`, Supabase `kspgtupzjzdskeonnvvu`, E2E `e2e_bulk 20/20`, `e2e_mobile_full 50/50`.
